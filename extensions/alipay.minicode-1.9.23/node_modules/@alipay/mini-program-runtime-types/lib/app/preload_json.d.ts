/**
 * 请参考: https://yuque.antfin-inc.com/exthub/uvrufx/fsdpno
 */
declare type PreloadJsonItem = {
    type: string;
    params: Record<string, any>;
};
declare type PreloadJsonFetchItem = {
    /**
     * 数据预拉取类型
     */
    fetchType: 'pre';
    params: {
        /**
         * HTTP 请求的 URL 地址
         */
        url: string;
        /**
         * http method. 支持 GET/POST/DELETE/PUT
         */
        method: 'GET' | 'POST' | 'DELETE' | 'PUT';
        /**
         * 可选。请求的参数，如果是 method 为 GET, 会将参数做成 query 拼接在 url 后
         */
        data?: Record<string, any>;
        /**
         * 可选。额外的header信息。
         */
        headers?: Record<string, any>;
        /**
         * 期望返回的数据格式，默认 json. 支持 json/text/base64/arraybuffer
         * @default json
         */
        dataType?: 'json' | 'text' | 'base64' | 'arraybuffer';
    };
} | {
    /**
     * JSAPI 预拉取类型
     * 目前只支持地理位置预拉取: https://opendocs.alipay.com/mini/05nbqx
     */
    fetchType: 'jsapiPre';
    params: {
        /**
         * JSAPI 名称
         * @example "my.getLocation"
         */
        jsapi: string;
        /**
         * 可选。请求的参数，参考 my.getLocation 入参，未填写则为空，不支持回调函数入参
         */
        data?: Record<string, any>;
    };
};
/**
 * 请在 mini.project.json 文件中配置 include 路径，确保 preload.json 能够被打进包产物中。
 * 请参考: https://opendocs.alipay.com/mini/02sd57
 */
export declare type TPreloadJson = (PreloadJsonItem | PreloadJsonFetchItem)[];
export {};
//# sourceMappingURL=preload_json.d.ts.map