import { IAppxWindowConfig } from '../shared';
/**
 * 请参考: https://opendocs.alipay.com/mini/framework/page-json
 */
export interface IPageJson extends IAppxWindowConfig {
    usingComponents?: Record<string, string>;
    /**
     * 懒加载模式会在实际渲染到插件组件时，自动去触发插件加载，所以在未下载或注入之前，插件组件处于不可用的状态，通过为插件的自定义组件设置 [占位组件](https://opendocs.alipay.com/mini/03dyb0)，可以先渲染占位组件作为替代，在插件下载完成后再进行替换。
     *
     * 详情请查看：[占位组件](https://opendocs.alipay.com/mini/03dyb0)
     */
    componentPlaceholder?: Record<string, string>;
    /**
     * 开启小程序页面 Web 托管
     * @doNotSuggest
     */
    webHosting?: boolean;
    /**
     * 默认情况下，页面的样式将对外产生影响。从基础库版本 2.7.2 开始，可以配置页面的样式隔离效果，避免页面的样式影响到外部。
     *
     * - `shared` 表示 app.acss 样式以及其他（设置了 shared 的其他页面和自定义组件）的样式将影响到当前页面，当前页面 acss 中指定的样式也会影响到外部。
     * - `apply-shared` app.acss 样式以及其他（设置了 shared 的其他页面和自定义组件）的样式将影响到当前页面，但当前页面 acss 中指定的样式不会影响外部。
     *
     * @default "shared"
     */
    styleIsolation?: 'apply-shared' | 'shared';
}
//# sourceMappingURL=page_json.d.ts.map