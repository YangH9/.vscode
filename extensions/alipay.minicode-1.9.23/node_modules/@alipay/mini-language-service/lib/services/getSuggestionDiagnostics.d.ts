/**
 * 参考文章：https://www.nieknijland.nl/blog/how-to-write-a-diagnostics-typescript-language-service-plugin
 */
import { ILogger, IMiniLanguageServiceContext, ITSUtils } from '../types';
import { DiagnosticWithLocation, CallExpression } from 'typescript/lib/tsserverlibrary';
import type * as ts from 'typescript/lib/tsserverlibrary';
export declare class SuggestionDiagnosticsProvider {
    private context;
    ts: typeof ts;
    tsUtils: ITSUtils;
    logger: ILogger;
    constructor(context: IMiniLanguageServiceContext);
    processMyRouterApiCallUrl(node: CallExpression): {
        url: string;
        start: number;
        width: number;
    };
    getRouterDiagnostics(fileName: string): DiagnosticWithLocation[];
    getSuggestionDiagnostics(fileName: string): DiagnosticWithLocation[];
}
