export declare type TConstructor<T, U extends unknown[] = unknown[]> = new (...args: U) => T;
export declare type IntersectSoft<A, B> = {
    [P in keyof A & keyof B]: A[P] | B[P];
};
export declare type IntersectHard<A, B> = {
    [P in keyof A & keyof B]: A[P] & B[P];
};
//# sourceMappingURL=common.d.ts.map