import { API, APIType } from '../../type';
export interface TypeExport<S> {
}
/**
 * @name 运动数据
 */
export interface APIExport<S> {
    [API.getRunData]: getRunData;
}
/**
 * @description 获取用户一个自然天内的运动步数信息。详情可参见 [运动数据](https://opendocs.alipay.com/mini/introduce/rundata) 能力介绍。
 * - 调用本 API 时无需再接入授权 API，系统将自动会检查用户是否已授权。若用户尚未授权，则会弹出授权框；用户同意授权后，可获取到返回的加密数据。 然后在服务端结合签名算法和 AES 密钥进行解密，获取运动数据，方法详见 [敏感信息加解密方法](https://opendocs.alipay.com/mini/introduce/aes)。
 * - 目前只支持查询最近 30 天内的运动数据，若超过 30 天，则返回的步数信息为 0。
 */
interface getRunData {
    type: APIType.Async;
    request: {
        /**
         * @description 要查询的步数日期（`yyyy-mm-dd`）的字符串，例如：`'2018-12-19'`。
         */
        countDate: string;
    };
    response: {
        /**
         * @description 查询到的指定日期的步数，该数据是经过加密的。
         * - 前端需要将该报文发送到开发者服务端做验签和解密处理（详细的服务端处理流程参考 [敏感信息加解密方法](https://opendocs.alipay.com/mini/introduce/aes)）
         * - 服务端解密后的数据格式为：`{ "count": "16880", "countDate": "2018-12-19", "code": "10000" };`
         */
        response: string;
    };
}
export {};
//# sourceMappingURL=run.d.ts.map