import { API, APIType } from '../../type';
export interface TypeExport<S> {
}
/**
 * @name 标题栏
 */
export interface APIExport<S> {
    [API.setTransparentTitle]: setTransparentTitle;
    [API.setNavigationBar]: setNavigationBar;
    [API.getTitleColor]: getTitleColor;
    [API.onTitleCloseClick]: onTitleCloseClick;
    [API.offTitleCloseClick]: offTitleCloseClick;
    [API.onTitleMoreClick]: onTitleMoreClick;
    [API.offTitleMoreClick]: offTitleMoreClick;
    [API.showNavigationBarLoading]: showNavigationBarLoading;
    [API.hideNavigationBarLoading]: hideNavigationBarLoading;
    [API.hideDebugVersion]: hideDebugVersion;
}
/**
 * @description 设置导航栏背景透明模式。
 */
interface setTransparentTitle {
    type: APIType.Async;
    request: {
        /**
         * @description 导航栏背景模式。
         */
        transparentTitle: `${ETransparentTitle}`;
    };
    response: void;
}
declare const enum ETransparentTitle {
    /**
     * @description 背景透明度自动变化模式。
     */
    auto = "auto",
    /**
     * @description 背景永久透明模式。
     */
    always = "always",
    /**
     * @description 背景不透明模式。
     */
    none = "none"
}
/**
 * @description 设置导航栏样式（包括：导航栏标题、导航栏背景色、导航栏底部边框颜色、导航栏左上角 logo 图片）。
 * - 导航栏左上角 logo 图片支持 gif 格式，必须使用 HTTPS 图片链接。
 * - 若设置了导航栏背景色 backgroundColor，则导航栏底部边框颜色 borderBottomColor  不会生效，默认会和 backgroundColor 颜色一样。
 * - 导航栏背景色不支持渐变色
 */
interface setNavigationBar {
    type: APIType.Async;
    request: {
        /**
         * @description 导航栏标题。
         */
        title?: string;
        /**
         * @description 图片链接地址（支持 gif 格式图片），必须是 HTTPS，请使用 iOS @3x 分辨率标准的高清图片。
         * - **若设置了 image 则 title 参数失效**。
         */
        image?: string;
        /**
         * @description 导航栏背景色，支持十六进制颜色值。
         */
        backgroundColor?: string;
        /**
         * @description 导航栏底部边框颜色，支持十六进制颜色值。
         * - **若设置了 backgroundColor，则 borderBottomColor  不会生效**，默认会和 backgroundColor 颜色一样。
         */
        borderBottomColor?: string;
        /**
         * @description 是否重置导航栏为支付宝默认配色。
         * @default false
         */
        reset?: boolean;
    };
    response: void;
}
/**
 * @description 获取导航栏背景色。
 */
interface getTitleColor {
    type: APIType.Async;
    request: void;
    response: {
        /**
         * @description 返回当前导航栏背景色。ARGB 格式的十六进制颜色值，如 `#323239FF`。
         */
        color: string;
    };
}
/**
 * @description 监听小程序右上角关闭按钮的点击事件。
 */
interface onTitleCloseClick {
    type: APIType.Event;
    event: void;
}
/**
 * @description 停止监听小程序右上角关闭按钮的点击事件。
 */
interface offTitleCloseClick {
    type: APIType.Event;
    event: void;
}
/**
 * @description 监听小程序右上角菜单按钮（...）的点击事件。
 */
interface onTitleMoreClick {
    type: APIType.Event;
    event: void;
}
/**
 * @description 停止监听小程序右上角菜单按钮的点击事件。
 */
interface offTitleMoreClick {
    type: APIType.Event;
    event: void;
}
/**
 * @description 在当前页面显示导航条的加载动画
 */
interface showNavigationBarLoading {
    type: APIType.Event;
    event: void;
}
/**
 * @description 在当前页面隐藏导航条的加载动画
 */
interface hideNavigationBarLoading {
    type: APIType.Event;
    event: void;
}
/**
 * @description 关闭调试功能
 * - 默认情况下，连续点击标题7次会弹出当前运行时的小程序框架版本号+构建时间 以及 小程序的发布标识。
 * - 对于个别小程序，如果不希望小程序具有如上功能，可以进行调用此API关闭。
 */
interface hideDebugVersion {
    type: APIType.Fn;
    fn: () => void;
}
export {};
//# sourceMappingURL=title.d.ts.map