import { API, APIType, FlagDef, Flag } from '../../type';
export interface TypeExport<S> {
}
/**
 * @name 滚动
 */
export interface APIExport<S> {
    [API.pageScrollTo]: pageScrollTo<S>;
}
/**
 * @description 滚动到页面的目标位置
 * - `scrollTop` 的优先级比 `selector` 高。
 * - 使用此 API 跳转小程序顶部时，必须将 `scrollTop` 值设为大于 `0`，方可实现跳转。
 * - 当传入 `selector` 参数，框架会执行 `querySelector(selector)` 以选取目标节点。
 */
interface pageScrollTo<S> {
    type: APIType.Async;
    request: {
        /**
         * @description 滚动到页面的目标位置，单位为 px。使用此 API 跳转小程序顶部时，必须将 scrollTop 值设为大于 0，方可实现跳转。
         */
        scrollTop?: number;
        /**
         * @description 滚动动画的时长，单位为 ms（毫秒）。
         * @default 0
         */
        duration?: FlagDef<S, number, Flag.Scroll.PageScrollToDuration>;
        /**
         * @description 选择器
         */
        selector?: FlagDef<S, string, Flag.Scroll.PageScrollToSelector>;
    };
}
export {};
//# sourceMappingURL=scroll.d.ts.map