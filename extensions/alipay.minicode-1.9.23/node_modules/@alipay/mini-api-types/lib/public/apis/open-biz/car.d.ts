import { API, APIType } from '../../type';
export interface TypeExport<S> {
}
/**
 * @name 车主服务
 * @description 该系列 API 目前仅支持 **支付宝** 客户端内使用，推荐优先使用 `my.ap.*` 业务域，不推荐使用 `my.*` 顶级域
 */
export interface APIExport<S> {
    [API.openCarService]: openCarService;
    [API.ap$openCarService]: ap$openCarService;
}
interface openCarService {
    type: APIType.Alias;
    alias: API.ap$openCarService;
}
/**
 * @description 打开支付宝内的车主服务页面。
 */
interface ap$openCarService {
    type: APIType.Async;
    request: {
        /**
         * @description 服务类型 id。
         * 有效值：
         * - `'0'` 保养服务
         * - `'1'` 爱车估值
         * - `'2'` 加油充值
         */
        serviceId: string;
        /**
         * @description 地址码，比如 `110100`。
         * @default 当前城市地址码
         */
        addressCode?: string;
    };
}
export {};
//# sourceMappingURL=car.d.ts.map