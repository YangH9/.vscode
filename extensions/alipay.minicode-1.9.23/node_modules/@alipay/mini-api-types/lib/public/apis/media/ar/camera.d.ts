import { APIType, Type } from '../../../type';
export interface TypeExport<S> {
    [Type.AR.ARContextGetCameraPosition]: getCameraPosition<S>;
    [Type.AR.ARContextSwitchCameraPosition]: switchCameraPosition<S>;
}
/**
 * @name 相机控制
 * @description
 * - 相关接口请先通过 `createARContext` 获取 `ARContext` 后，才能调用 `ARContext` 内的接口。
 */
export interface APIExport<S> {
}
/**
 * @description 【相机控制】获取当前设备摄像头位置。
 */
interface getCameraPosition<S> {
    type: APIType.Async;
    request: void;
    response: {
        /**
         * @description 摄像头前后置位置。
         * - `0`：后置。
         * - `1`：前置。
         */
        position: 0 | 1;
    };
}
/**
 * @description 【相机控制】转换摄像头前后置位置。
 */
interface switchCameraPosition<S> {
    type: APIType.Async;
    request: {
        /**
         * @description 摄像头前后置位置。
         * - `0`：后置。
         * - `1`：前置。
         */
        position: 0 | 1;
    };
}
export {};
//# sourceMappingURL=camera.d.ts.map