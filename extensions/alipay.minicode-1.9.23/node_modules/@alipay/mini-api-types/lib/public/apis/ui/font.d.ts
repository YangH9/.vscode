import { API, APIType } from '../../type';
export interface TypeExport<S> {
}
/**
 * @name 字体
 */
export interface APIExport<S> {
    [API.loadFontFace]: loadFontFace;
}
/**
 * @description 动态加载网络字体的 API。
 * 注意事项：
 * - 文件地址需为下载类型。
 * - `iOS` 仅支持 `HTTPS` 格式文件地址。
 * - 支付宝小程序目前只支持 `woff`，`otf`，`ttf`，`sfnt` 字体。
 * - 支付宝小程序不支持 `woff2` 字体。
 */
interface loadFontFace {
    type: APIType.Async;
    request: {
        /**
         * @description 字体名称。
         */
        family: string;
        /**
         * @description 字体资源地址。
         */
        source: string;
        /**
         * @description 字体描述符。
         */
        desc?: Desc;
    };
}
interface Desc {
    /**
     * @description 字体样式。
     * 可选值为：
     * - `normal`
     * - `italic`
     * - `oblique`
     * @default normal
     */
    style?: string;
    /**
     * @description 字体粗细，可选值为 `normal` / `bold` / `100` / `200` / ... / `900`。
     * @default normal
     */
    weight?: string;
    /**
     * @description 设置小型大写字母的字体显示文本。
     * 可选值为：
     * - `normal`
     * - `small-caps`
     * - `inherit`
     * @default normal
     */
    variant?: string;
}
export {};
//# sourceMappingURL=font.d.ts.map