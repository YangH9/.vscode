import { API, APIType } from '../../type';
export interface TypeExport<S> {
}
/**
 * @name 收货地址
 */
export interface APIExport<S> {
    [API.chooseAddress]: chooseAddress;
    [API.ap$chooseAddress]: ap$chooseAddress;
    [API.getAddress]: getAddress;
    [API.ap$getAddress]: ap$getAddress;
}
/**
 * @description 调起用户的支付宝收货地址界面，并在用户点击选择后返回用户选择的地址 id。
 * - 需要用户授权 `user_address`，授权请参考 `getAuthCode` api。
 */
interface chooseAddress {
    type: APIType.Async;
    request: void;
    response: {
        /**
         * @description 用户选择的地址 id
         */
        addressId: string;
    };
}
interface ap$chooseAddress {
    type: APIType.Alias;
    alias: API.chooseAddress;
}
/**
 * @description 商户在寄送外卖、快递或其他场景需要用户填写地址信息时，可通过调用此 API 直接获取地址数据，无需用户手动填写。详情可参见 [获取会员收货地址](https://opendocs.alipay.com/mini/introduce/getaddress) 能力介绍。
 * - 调用接口时会弹出授权框，用户点击弹框按钮同意授权后，可以通过接口获取到返回的收货地址数据。若用户未授权，则无法返回正确信息。
 */
interface getAddress {
    type: APIType.Async;
    request: void;
    response: {
        /**
         * @description 详细地址
         */
        address: string;
        /**
         * @description 国家名称
         */
        country: string;
        /**
         * @description 省
         */
        prov: string;
        /**
         * @description 市
         */
        city: string;
        /**
         * @description 区
         */
        area: string;
        /**
         * @description 街道
         */
        street: string;
        /**
         * @description 名称
         */
        fullname: string;
        /**
         * @description 手机号
         */
        mobilePhone: string;
    };
}
interface ap$getAddress {
    type: APIType.Alias;
    alias: API.getAddress;
}
export {};
//# sourceMappingURL=address.d.ts.map