import { API, APIType, Flag, FlagDef } from '../../type';
export interface TypeExport<S> {
}
/**
 * @name 快照
 * @description 关于 Snopshot 更多详细的运行机制，请参考 [首屏快照指南](https://yuque.antfin-inc.com/appxng/guide/snapshot-2.0)
 */
export interface APIExport<S> {
    [API.enableSnapshot]: enableSnapshot<S>;
    [API.reportSnapshot]: reportSnapshot<S>;
    [API.saveSnapshot]: saveSnapshot<S>;
}
/**
 * @description 开启首屏快照。
 */
interface enableSnapshot<S> {
    type: APIType.Sync;
    request: {
        /**
         * @description 数据等到超时时间，超时后立即更新页面，单位：毫秒
         */
        timeout: number;
        /**
         * @description 是否强制跳过动态插件检查。
         * - 如果小程序使用了动态插件，则禁止快照
         * - 如果你确定首页没有用到任何动态插件的内容，可以通过此参数跳过检查
         */
        disableDynamicPluginCheck?: FlagDef<S, boolean, Flag.Performance.EnableSnapshotDisableDynamicPluginCheck>;
    };
}
/**
 * @description 存/删快照。
 */
interface reportSnapshot<S> {
    type: APIType.Sync;
    request: {
        /**
         * @description 数据是否可用。
         * - `false` 数据不可用，删除快照
         * - `true` 数据可用，保存快照
         */
        available: boolean;
    };
}
/**
 * @description 保存当前页面快照。
 */
interface saveSnapshot<S> {
    type: APIType.Fn;
    fn: () => void;
}
export {};
//# sourceMappingURL=snapshot.d.ts.map