import { API, APIType } from '../../type';
export interface TypeExport<S> {
}
/**
 * @name 设备授权
 */
export interface APIExport<S> {
    [API.showAuthGuide]: showAuthGuide;
    [API.getAuthStatus]: getAuthStatus;
}
declare const enum EAuthType4Android {
    /**
     * @description 后台保活权限 (Android)
     */
    BACKGROUNDER = "BACKGROUNDER",
    /**
     * @description 桌面快捷权限 (Android)
     */
    SHORTCUT = "SHORTCUT",
    /**
     * @description Push 通知栏权限 (Android)
     */
    NOTIFICATION = "NOTIFICATION",
    /**
     * @description 自启动权限 (Android)
     */
    SELFSTARTING = "SELFSTARTING"
}
declare const enum EAuthType4IOS {
    /**
     * @description 麦克风权限 (iOS)
     */
    MICROPHONE = "MICROPHONE",
    /**
     * @description 通讯录权限 (iOS)
     */
    ADDRESSBOOK = "ADDRESSBOOK",
    /**
     * @description 相机权限 (iOS)
     */
    CAMERA = "CAMERA",
    /**
     * @description 照片权限 (iOS)
     */
    PHOTO = "PHOTO",
    /**
     * @description LBS 总开关 (iOS)
     */
    LBSSERVICE = "LBSSERVICE",
    /**
     * @description LBS 开关（应用级别） (iOS)
     */
    LBS = "LBS"
}
/**
 * @description 通过权限引导模块以图文等形式向用户弹出 Dialog，引导用户打开相应的权限
 */
interface showAuthGuide {
    type: APIType.Async;
    request: {
        /**
         * @description 引导的权限标识，用于标识该权限类型(如 LBS)。
         */
        authType: `${EAuthType4IOS}` | `${EAuthType4Android}`;
    };
}
/**
 * @description 获取权限状态，用于判断【自启】，【后台运行】，【定位】以及【通知栏开关】的权限状态。
 */
interface getAuthStatus {
    type: APIType.Async;
    request: {
        /**
         * @description 请求权限业务来源，需要申请。
         */
        bizType: string;
        /**
         * @description 权限类型。
         */
        authType: `${EAuthType4IOS}` | `${EAuthType4Android}`;
    };
    response: {
        /**
         * @description 权限状态。
         * - `"1"` 为权限打开
         * - 其余均为权限关闭
         */
        authStatus: string;
    };
}
export {};
//# sourceMappingURL=device-auth.d.ts.map