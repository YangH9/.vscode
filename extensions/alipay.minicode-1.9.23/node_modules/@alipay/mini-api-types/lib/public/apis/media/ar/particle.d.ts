import { APIType, Type } from '../../../type';
export interface TypeExport<S> {
    [Type.AR.ARContextEmitParticle]: emitParticle<S>;
}
/**
 * @name 粒子动画(图形渲染)
 * @description 本文主要介绍 **图形渲染** 方面的 **粒子动画** 相关接口。
 * - 相关接口请先通过 `createARContext` 获取 `ARContext` 后，才能调用 `ARContext` 内的接口。
 */
export interface APIExport<S> {
}
/**
 * @description 【粒子动画】粒子发射开关。
 */
interface emitParticle<S> {
    type: APIType.Async;
    request: {
        /**
         * @description 粒子节点id。
         */
        nodeId: string;
        /**
         * @description 是否发射粒子。
         */
        active: boolean;
    };
}
export {};
//# sourceMappingURL=particle.d.ts.map