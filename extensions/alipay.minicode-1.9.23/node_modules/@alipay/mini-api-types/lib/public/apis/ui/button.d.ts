import { API, APIType } from '../../type';
export interface TypeExport<S> {
}
/**
 * @name 导航按钮
 */
export interface APIExport<S> {
    [API.setBackButton]: setBackButton;
    [API.showBackToHomepage]: showBackToHomepage;
    [API.hideBackHome]: hideBackHome;
    [API.setCustomPopMenu]: setCustomPopMenu;
    [API.hideAddToDesktopMenu]: hideAddToDesktopMenu;
    [API.hideAllAddToDesktopMenu]: hideAllAddToDesktopMenu;
    [API.setOptionMenu]: setOptionMenu;
    [API.getMenuButtonBoundingClientRect]: getMenuButtonBoundingClientRect;
}
/**
 * @description 设置导航栏返回按钮
 * @tutorial api setBackButton
 * @file
 * ```js
 * my.setBackButton({ color: '#ffffff' });
 * ```
 */
interface setBackButton {
    type: APIType.Async;
    request: {
        /**
         * @description 返回按钮的颜色，HEXcolor 格式，如 `#FF0000`
         */
        color: string;
    };
    response: void;
}
/**
 * @description 显示返回首页功能
 */
interface showBackToHomepage {
    type: APIType.Async;
    request: void;
    response: {
        /**
         * @description 错误码，如 `7`
         */
        error: number;
        /**
         * @description 错误说明文案，错误码 `7` 则对应 `小程序首页无法添加此功能`
         */
        errorMsg: string;
    };
}
/**
 * @description 是隐藏标题栏上的返回首页图标（如下图所示）和右上角通用菜单中的返回首页功能的 API。
 * - 用户启动小程序时，若进入的页面不是小程序首页，则会在左上角出现返回首页图标。
 * - 如果 app.json 中配置了 tabbar 跳转 pages/index/index 时，不会出现 返回首页 功能。
 */
interface hideBackHome {
    type: APIType.Fn;
    fn: () => void;
}
/**
 * @description 右上角更多中，支持插入自定义菜单。
 */
interface setCustomPopMenu {
    type: APIType.Async;
    request: {
        /**
         * @description 自定义菜单列表
         */
        menus: CustomMenuItem[];
    };
}
/**
 * @description 自定义菜单列表
 */
interface CustomMenuItem {
    /**
     * @description 自定义菜单名称
     */
    name: string;
    /**
     * @description 自定义菜单的 icon URL，大小64x64
     */
    menuIconUrl: string;
}
/**
 * @description 隐藏当前页面通用菜单中的 **添加到桌面** 功能
 * @tutorial api hideAddToDesktopMenu
 * @file
 * ```js
 * my.hideAddToDesktopMenu();
 * ```
 */
interface hideAddToDesktopMenu {
    type: APIType.Fn;
    fn: () => void;
}
/**
 * @description 隐藏所有页面的通用菜单中的 添加到桌面 功能。
 */
interface hideAllAddToDesktopMenu {
    type: APIType.Fn;
    fn: () => void;
}
/**
 * @description 配置 `optionMenu` 导航栏额外图标，点击后触发 `onOptionMenuClick`。
 */
interface setOptionMenu {
    type: APIType.Async;
    request: {
        /**
         * @description 自定义 optionMenu 所用图标的 URL 或 Base64 图片。 图片尺寸建议为 30*30 px。 更多使用限制请参见 下方 icon 属性使用须知。
         */
        icon: string;
    };
    response: void;
}
/**
 * @description 获取菜单按钮（右上角胶囊按钮）的布局位置信息。
 */
interface getMenuButtonBoundingClientRect {
    type: APIType.Async;
    request: void;
    response: {
        /**
         * @description 是否成功
         */
        success: boolean;
        /**
         * @description 宽度，px
         */
        width: number;
        /**
         * @description 高度，px
         */
        height: number;
        /**
         * @description 顶部坐标
         */
        top: number;
        /**
         * @description 右边坐标
         */
        right: number;
        /**
         * @description 底部坐标
         */
        bottom: number;
        /**
         * @description 左边坐标
         */
        left: number;
    };
}
export {};
//# sourceMappingURL=button.d.ts.map