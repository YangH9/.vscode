module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  modulePathIgnorePatterns: ['<rootDir>/dist/', '<rootDir>/out/'],
  globals: {
    'ts-jest': {
      isolatedModules: true,
    },
  }
};
